<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://chen-yu-zheng.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://chen-yu-zheng.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-20T13:02:47+00:00</updated><id>https://chen-yu-zheng.github.io/feed.xml</id><title type="html">blank</title><subtitle>Home page of Chenyu Zheng, a Ph.D. student working on machine learning theory, optimization, and scalability in deep learning. </subtitle><entry><title type="html">ELDE：一种科学实用的 Critical Batch Size 估计方法</title><link href="https://chen-yu-zheng.github.io/blog/2026/CBS03/" rel="alternate" type="text/html" title="ELDE：一种科学实用的 Critical Batch Size 估计方法"/><published>2026-08-20T04:00:00+00:00</published><updated>2026-08-20T04:00:00+00:00</updated><id>https://chen-yu-zheng.github.io/blog/2026/CBS03</id><content type="html" xml:base="https://chen-yu-zheng.github.io/blog/2026/CBS03/"><![CDATA[<p>这篇博客基于之前介绍的两大类 Critical Batch Size（CBS）估计方法，博采众长，提出了一种科学实用的 CBS 估计方法。</p> <h2 id="前情回顾">前情回顾</h2> <p>在之前的两篇博客 [1,2] 中，我们详细介绍了 OpenAI 经典的 CBS 估计方法 [3] 和后续的一系列改进 [4, 5, 6, 7, 8]。我们先回顾一下这些方法的大致思想，以及目前仍然存在的问题。</p> <p>第一类是<strong>基于对齐目标 loss 的方法</strong>，OpenAI 经典的 CBS 估计方法 [3] 和目前的主流方法 [4,5] 都属于此类方法。为了估计 CBS，它们首先需要确定一个目标 loss，然后收集不同 batch size $B$ 下（其它超参经过调优）达到该目标 loss 所需的训练时间 $S$ 和训练数据 $E$，从而基于时间-数据权衡理论选择出一个平衡两者的 $B$ 作为 CBS。虽然这类方法理论支撑较为成熟，但是在 [2] 中我们已经提到，这类方法的一个问题是“目标 loss 选取不直观”：在面对一个新的训练场景时，我们可能并不清楚目标 loss 要达到多少才是最合理的；有时候更实际的需求是在给定训练规模（模型大小 $N$ 和数据量 $E$）的情况下，确定一个 CBS。</p> <p>第二类是<strong>基于对齐训练规模的方法</strong>，近期一些工作 [6,7,8] 采取了这样的思想。它们往往是在给定训练规模（模型大小 $N$ 和数据量 $E$）的情况下，换不同的 $B$ 进行训练（其它超参经过调优）并收集 loss，然后找到一个 loss 出现拐点、达到某种容忍界的 $B$ 作为 CBS。相较于第一类方法，这类方法面向了更加实际的场景，但其问题在于拐点的选取相当主观、没有科学的理论支撑，比如 [6] 直接靠肉眼评估拐点，[7] 定义拐点为 $L(B_{crit}) - L(B_{opt}) = 0.01$。除此之外，随着数据分布或者训练规模的变化，loss 的性质也会发生变化，loss 拐点选取的标准如何随之改变也难以确定。</p> <p>在这篇博客中，我们基于前人的工作，博取两类方法之所长，提出一种既面向实际需求又有理论支撑的 CBS 估计方法。</p> <h2 id="elde-equal-loss-data-efficiency等损失数据效率法">ELDE: Equal-Loss Data Efficiency/等损失数据效率法</h2> <p>在这一节，我们详细说明我们的 CBS 估计方法 ELDE，其与已有方法的联系放到下一节进行说明。</p> <p>首先，我们明确我们想解决的问题：<strong>在给定训练规模（模型大小 $N$ 和数据量 $E$）和 lr schedule（如 10% warmup + 90% cosine decay）的情况下，估计训练的 CBS</strong>。注意到，和 [5] 一样，我们假设实际中已经选好了一个 lr schedule，从而规避了对不同 $B$ 调优 lr schedule 的难题，相关的合理性讨论可以见 [2]。</p> <p>我们再对 notation 进行一些说明。假设 $B$ 为当前尝试的 bs，$B_{opt}$ 为最优 bs 或者远离 CBS、处于 linear scaling regime 的基准 bs [2,4]。$L_{N,B}(E)$ 为给定模型大小 $N$ 和 bs $B$ 下，经过超参调优，训练 $E$ 数据量后达到的 loss。</p> <p>我们的方法是从“基于对齐训练规模的方法”出发的，但是规避了其中“基于 loss difference $L_{N,B}(E) - L_{N,B_{opt}}(E)$ 选取拐点”的不科学之处。我们方法的基本思想是将 loss difference 转化为 equal-loss data efficiency (ELDE) 的 gap，从而与 “基于对齐目标 loss 的方法” 的理论体系建立联系。大体来看，在实际操作中，我们首先估计 $B_{opt}$ 达到当前 $B$ 的性能 $L_{N,B}(E)$ 所需的等效数据量 $E^{eq}$。然后，我们计算 $\rho = E^{eq}/E$ 。可以看出， $\rho$ 越小，意味着当前 $B$ 训练的数据效率越低。因此，我们便把数据效率 $\rho$ 小到某个临界值的 $B$ 记为 CBS。</p> <p>我们接下来详细介绍所提的 ELDE 方法。首先，为了能够估计 $B_{opt}$ 达到当前 $B$ 的性能 $L_{N,B}(E)$ 所需的等效数据量 $E^{eq}$，我们需要先对 $B_{opt}$ 拟合一个 loss-data 关系。注意，由于 lr schedule 的存在，这里不能简单地用 $B_{opt}$ 训练数据量 $E$ ，然后看 $L_{N,B}(E)$ 位于这条曲线的什么位置。受到 [5] 启发，我们可以固定 $B_{opt}$ 训练不同的数据量，然后拟合：</p> \[\widehat{L}_{N,B_{opt}}(E) = L_N + C_{N,B_{opt}} E^{-\beta_{N,B_{opt}}}.\] <p>基于这个关系，我们就可以得到 $B$ 对应的等效数据量和数据效率：</p> \[E^{eq}_{N,E}(B) = \left(\frac{C_{N,B_{opt}}}{L_{N,B}(E) - L_N}\right)^{\frac{1}{\beta_{N,B_{opt}}}}, \quad \rho_{N,E}(B) = \frac{E^{eq}_{N,E}(B)}{E}.\] <p>最后，我们定义模型大小 $N$ 和数据量 $E$ 对应的 CBS 为</p> \[B_{crit}(N,E) = \sup\{B: \rho_{N,E}(B) \ge \rho_* \},\] <p>其中 $\rho_*$ 为人为设置的数据效率容忍量，如 0.9；$B_{crit}$ 可以通过 sweep $B$ 来进行判断。总的来说，为了估计给定训练规模和 lr schedule 情况下的 CBS，我们所提的 ELDE 算法可以概括为：</p> <ol> <li>选择一个最优 bs 或者远离 CBS、处于 linear scaling regime [2,4] 的基准 bs $B_{opt}$；</li> <li>固定 $B_{opt}$，换不同的数据量，调参，训练，收集最终 loss 并拟合 \(\widehat{L}_{N,B_{opt}}(E) = L_N + C_{N,B_{opt}} E^{-\beta_{N,B_{opt}}}\)；</li> <li>逐渐增大 $B$，训练 $E$ 数据量，收集最终 loss $L_{N,B}(E)$ 并计算对应的数据效率 $\rho_{N,E}(B)$；</li> <li>对比 $\rho_{N,E}(B)$ 与 $\rho_*$，获得 CBS 估计量 $B_{crit}(N,E)$。</li> </ol> <h2 id="与已有方法的联系和区别">与已有方法的联系和区别</h2> <p>这一节，我们来谈一谈 ELDE 与已有方法的联系和区别。</p> <h3 id="基于对齐目标-loss-的方法">基于对齐目标 loss 的方法</h3> <p>首先是“基于对齐目标 loss 的方法” [3,4,5]。这些方法本质上都是使用 OpenAI 的经典时间-数据权衡理论 [1,3]，所以我们先阐明 ELDE 与 OpenAI 经典理论的关系，再谈谈与各种具体方法的区别。</p> <p><strong>我们现在证明，ELDE 选取的 CBS 也对应着 OpenAI 经典的时间-数据权衡理论，因此所得到的估计量享有同样的理论支撑</strong>。回顾博客 [1] 中的理论推导可以知道，为了达到目标 loss，$B$ 对应的训练步数 $S(B)$ 和数据耗费 $E(B)$ 满足如下关系：</p> \[\begin{aligned} &amp; {S(B)} = {S_{min}} + \frac{E_{min}}{B}, \\ &amp; {E(B)} = E_{min} + B {S_{min}}, \\ &amp; \frac{E(B)}{E_{min}} - 1 = \left(\frac{S(B)}{S_{min}} - 1\right)^{-1}. \end{aligned}\] <p>那么为了达到目标 loss $L_{N,B}(E)$，基于上式我们有：</p> \[\begin{aligned} &amp; E = E_{min} + B {S_{min}}, \\ &amp; E^{eq} = E_{min} + B_{opt} S_{min}. \end{aligned}\] <p>我们基于该方程组可以解出：</p> \[E_{min} = \frac{BE^{eq} - B_{opt}E}{B - B_{opt}}, \quad S_{min} = \frac{E - E^{eq}}{B- B_{opt}}.\] <p>因此，OpenAI 中的 CBS 估计可以写为：</p> \[B_{crit-oai} = \frac{E_{min}}{S_{min}} = \frac{BE^{eq} - B_{opt}E}{E - E^{eq}} = \frac{BE^{eq}/E - B_{opt}}{1 - E^{eq}/E}.\] <p>当 $B$ 为 ELDE 估计的 CBS 时，我们有</p> \[B_{crit-oai} = \frac{B_{crit-ELDE}\rho_* - B_{opt}}{1 - \rho_*} \approx \frac{\rho_*}{1 - \rho_*} B_{crit-ELDE}.\] <p>因此，两种估计方法的结果之间近似只差一个倍数，这本质上反映的是两种方法在训练时间-训练数据上的权衡不同。事实上，对于训练时间，我们有</p> \[S(B_{crit-ELDE}) \approx \frac{1}{1 - \rho} S_{min}, \quad S(B_{crit-oai}) = 2S_{min}.\] <p>对于训练数据，我们有</p> \[E(B_{crit}) \approx \frac{1}{\rho} E_{min}, \quad S(B_{crit-oai}) = 2 E_{min}.\] <p>上式表明，$\rho_\ast$ 越大，时间-数据权衡的选择上更重视数据耗费，而不是训练时间。特别地，当 $\rho_\ast = 1/2$ 时，$B_{crit-ELDE} \approx B_{crit-oai}$，能够得到与原论文类似的结果。</p> <p>到目前为止，我们已经证明了 ELDE 与 OpenAI 经典理论的紧密联系，从而享有较好的理论支撑。在实践上，ELDE 的一大好处是不用预先设定目标 loss。除此之外，ELDE 相较于具体方法 [4,5] 还有如下好处：</p> <ol> <li>与 [4] 对比：可以直接在我们感兴趣的 lr schedule 上做实验，而不是用 constant lr + EWA。</li> <li>与 [5] 对比：只需要对 $B_{opt}$ 拟合 loss-data 曲线，而 [5] 需要对所有的 $B$ 都拟合该曲线。</li> </ol> <h3 id="基于对齐训练规模的方法">基于对齐训练规模的方法</h3> <p>ELDE 本身也属于此类方法。但相较于已有的方法 [6,7,8]，ELDE 拥有好的理论支撑，克服了“基于 loss difference 选取拐点”的不科学之处，能够更方便地泛化至不同的训练场景。</p> <h2 id="总结">总结</h2> <p>这篇博客基于之前的两大类 CBS 估计方法，博采众长，提出了一种更加科学实用的 CBS 估计方法。</p> <h2 id="参考文献">参考文献</h2> <ol> <li>经典回顾：OpenAI Critical Batch Size, https://chen-yu-zheng.github.io/blog/2026/CBS/</li> <li>论文阅读：一些实用的 Critical Batch Size 估计方法, https://chen-yu-zheng.github.io/blog/2026/CBS02/</li> <li>An Empirical Model of Large-Batch Training, OpenAI, 2018</li> <li>How Does Critical Batch Size Scale in Pre-training?, ICLR, 2025</li> <li>Power Lines: Scaling Laws for Weight Decay and Batch Size in LLM Pre-training, NeurIPS, 2025</li> <li>The Design Space of Tri-Modal Masked Diffusion Models, 2026</li> <li>Critical Batch Size Revisited: A Simple Empirical Approach to Large-Batch Language Model Training, NeurIPS, 2025</li> <li>DeepSeek LLM Scaling Open-Source Language Models with Longtermism, 2024</li> </ol> <h2 id="引用">引用</h2> <p>如果您需要引用本文，请参考：</p> <div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@online<span class="p">{</span>zheng2026ELDE,
  title   = <span class="p">{</span>ELDE：一种科学实用的 Critical Batch Size 估计方法<span class="p">}</span>,
  author  = <span class="p">{</span>Chenyu Zheng<span class="p">}</span>,
  year    = <span class="p">{</span>2026<span class="p">}</span>,
  url     = <span class="p">{</span>https://chen-yu-zheng.github.io/blog/2026/CBS03/<span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="optimization"/><category term="scaling"/><category term="batch-size"/><summary type="html"><![CDATA[基于之前介绍的两大类 CBS 估计方法，博采众长，提出了一种科学实用的 CBS 估计方法。]]></summary></entry><entry><title type="html">论文阅读：一些实用的 Critical Batch Size 估计方法</title><link href="https://chen-yu-zheng.github.io/blog/2026/CBS02/" rel="alternate" type="text/html" title="论文阅读：一些实用的 Critical Batch Size 估计方法"/><published>2026-08-16T15:00:00+00:00</published><updated>2026-08-16T15:00:00+00:00</updated><id>https://chen-yu-zheng.github.io/blog/2026/CBS02</id><content type="html" xml:base="https://chen-yu-zheng.github.io/blog/2026/CBS02/"><![CDATA[<p>这篇博客提出 OpenAI 原始 critical batch size（CBS）估计方法的两个问题，然后选取介绍了一些近年出现的更加方便实用的 CBS 估计方法。</p> <h2 id="前情回顾">前情回顾</h2> <p>在上一篇文章 [1] 中，我们详细介绍了 OpenAI 在 critical batch size（CBS）上的经典工作 [2]。针对实际训练中 CBS 的估计，该文章提出了一个基于训练时间-数据消耗的双曲线估计方法，我们先对其实操流程做一个回顾：</p> <ol> <li>固定一个目标性能。CBS 是和 loss 相关的。通常来说，loss 越小，梯度信号通常相对越小，噪声相对越大，CBS 往往越大。因此，我们需要先固定一个目标性能，再来谈 CBS 的估计问题。</li> <li>扫描不同 bs，并对不同的 bs 扫描超参数（如 lr、lr schedule、动量、weight decay 等），目标是对每一个 bs 尽可能把优化过程调到最优，尽快达到目标 loss。</li> <li>对于每个 bs $B$，选择最快达到目标性能的 run，记录其训练步数 $S(B)$ 和数据耗费 $E(B)$。</li> <li>拟合形如 $(\frac{E(B)}{E_{min}} - 1) = (\frac{S(B)}{S_{min}} - 1)^{-1}$ 的时间-数据双曲线，然后用 $B_{crit} = \frac{E_{min}}{S_{min}}$ 来获得 CBS 的估计。</li> </ol> <p>然而，代入实际的训练场景中一想，就会发现这个方法实操起来难度还是比较大的，比如存在下面一些问题：</p> <ol> <li>目标性能的选取并不直观。在面对一个新任务的时候，我们可能并不清楚目标 loss 要达到多少才是最合理的，这会直接导致测出来的 CBS 可能并不是我们真正期待的。有时候，更贴合实际需求的是：给定一个模型大小 $N$ 和和数据量 $E$，我们如何确定一个 CBS？</li> <li>超参调优的难度非常大，且这里面问题最大的看起来是 lr schedule。因为我们需要针对一个目标 loss，选取超参使得达到目标 loss 的训练步数最小。而 lr schedule 是直接影响到训练步数的，且本质上直接蕴涵了多个子超参数，如 warmup 步数，stable 步数，decay 的步数和方式。如果对于每一个 bs，我们都要重新调这些步数直接相关的超参，是非常难的。</li> </ol> <p>这篇博客暂时不提出新的 CBS 估计策略，而是选取一些近年出现的更加方便实用的 CBS 估计方法进行介绍，这些方法都或多或少地缓解了所提到的两个问题。</p> <h2 id="how-does-critical-batch-size-scale-in-pre-training-3">How Does Critical Batch Size Scale in Pre-training? [3]</h2> <p>这篇文章考虑了在给定模型大小 $N$ 和和数据量 $E$ 的情况下，如何对 CBS 进行估计。相对于 OpenAI 原论文 [2]，虽然这篇文章对 CBS 的定义和估计方法都有一些改变，但和原来本质上是相似的。我们先介绍这篇文章的定义和方法，最后再来说明和 [2] 的关系。</p> <h3 id="文章所提方法">文章所提方法</h3> <p>首先，回顾上面的问题一，我们还是得需要选取一个目标 loss，不过此时我们可以让目标 loss 和 $N$、$E$ 两者挂钩，从而避免盲目的选择。一个自然的选择是给定 N、E 的情况下，模型能够达到的最优 loss，但是完美实现这一点需要大量的调参（bs、优化器的超参等）。从 [1] 的推导中可以知道，在 bs 达到 CBS 之前，相当范围内不同 bs 对应的调优效果是差不多的，所以实际中我们选取一个相对安全、远离 CBS 的 bs 作为基准 $B_{opt}$，然后在 $B_{opt}$ 下调其它超参，获得对应的最优 loss 作为目标 loss 就行了。</p> <p>回应第二个调参的问题，这篇文章经过比较系统的消融发现 constant lr + EWA 能够达到和常用的 lr schedule 类似的效果，所以文章采用 constant lr + EWA，从而丢弃了 warmup，stable，decay 等 lr schedule 超参数，只额外引入了一个 EWA 系数，降低了调参压力。当然，lr、动量、wd 等超参的调整仍然是无法避免的。</p> <p>此时，确认完问题一和问题二之后，我们其实已经可以按照 OpenAI 的方法做 CBS 的估计了，但文章换了一个 CBS 的定义，从而也在具体的估计环节和原来有所差异。回顾 [1] 中的理论模型推导，我们已经知道</p> \[\frac{S(B)}{S_{min}} - 1 = \frac{1}{B} \frac{E_{min}}{S_{min}} \implies S(B) = S_{min} + \frac{E_{min}}{B}.\] <p>当 B 较小，${E_{min}}/{B} \gg S_{min}$ 的时候，我们有 $S(B) \approx {E_{min}}/{B}$。此时，为了达到目标 loss，线性增大 B 可以带来近似线性的步数减小，消耗的总数据量近似不变，可以放心增大 B 来利用卡数，我们把这个称为 linear scaling regime。当 B 较大，${E_{min}}/{B} \ll S_{min}$ 的时候，增大 B 难以再有效降低迭代步数，数据效率降低。CBS 正是落在 linear scaling regime 与后者的临界点，文章也是基于这个想法提出了 CBS 的定义，后续我们看到，这个和 OpenAI 的思路其实本质上是相似的。</p> <p>设基准 $B_{opt}$ 达到目标 loss 的步数为 $S(B_{opt})$。我们为了得到 CBS，可以在此基础上继续增加 B 并调超参，如果仍然处于 linear scaling regime 中，我们期待达到目标 loss 的步数为 $S(B) \approx S(B_{opt}) \frac{B_{opt}}{B}$。当逐渐脱离 linear scaling regime 后，实际得到的 $S(B)$ 将会变得大于 $S(B_{opt}) \frac{B_{opt}}{B}$。为了反映这个临界点，我们设置一个容忍界限 $\beta$（原论文为 0.2），并定义 CBS 满足</p> \[S(B_{crit}) = (1+\beta)S(B_{opt}) \frac{B_{opt}}{B_{crit}}, \quad or\ i.e. \quad E(B_{crit}) = (1 + \beta)E(B_{opt}).\] <p>即 CBS 达到目标 loss 的训练步数是理想 linear scaling 的 $1 + \beta$ 倍。在实际中，比起一个个 B 试，更方便的是先拟合 $S(B) = S_{min} + \frac{E_{min}}{B}$（这和 [2] 中的双曲线拟合本质上一样！），然后代入 CBS 的定义中可以化简得到</p> \[B_{crit} = (1 + \beta)B_{opt} + \beta \frac{E_{min}}{S_{min}}.\] <p>这样我们就得到了给定模型大小 $N$ 和和数据量 $E$ 的情况下的一个 CBS 估计。注意到结论和 $B_{opt}$ 直接相关，且我们的推导都是基于 $B_{opt}$ 在 linear scaling regime 内的，所以实际中要选取一个安全的、比较远离 CBS 的值。最后，文章基于这个方法，可以探索 CBS 关于模型大小和训练数据量的关系，最终的结果为 CBS 和模型大小关系不大，但是会随着训练数据量的增大而增大，如下图所示：</p> <div class="row justify-content-center"> <div class="col-12 col-md-10 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-08-cbs02/cbs_token-480.webp 480w,/assets/img/2026-08-cbs02/cbs_token-800.webp 800w,/assets/img/2026-08-cbs02/cbs_token-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2026-08-cbs02/cbs_token.jpeg" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="CBS 主要和训练数据量相关" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <div class="caption"> 图 1：CBS 主要和训练数据量相关。 </div> </div> </div> <h3 id="和-openai-原论文的关系">和 OpenAI 原论文的关系</h3> <p>仔细思考一下，其实这篇论文的方法本质上和 OpenAI 论文 [2] 的方法是一样的，两者都是通过拟合 $(S_{min}, E_{min})$，只不过选取拟合的曲线不同，以及 CBS 对时间-数据的权衡不同。我们接下来简单说明一下。</p> <p>首先，从我们上一篇博客 [1] 中的理论模型推导中可以知道，训练步数 $S(B)$ 和数据耗费 $E(B)$ 满足如下关系：</p> \[\begin{aligned} &amp; {S(B)} = {S_{min}} + \frac{E_{min}}{B}, \\ &amp; {E(B)} = E_{min} + B {S_{min}}, \\ &amp; \frac{E(B)}{E_{min}} - 1 = \left(\frac{S(B)}{S_{min}} - 1\right)^{-1}. \end{aligned}\] <p>因此，要获得 $(S_{min}, E_{min})$ 的估计，这三种方式都可以被采用。[3] 这篇论文选用的是第一种，OpenAI 原论文 [2] 选用的是第三种。</p> <p>其次，观察当前的 CBS 估计结果，记 [2] 中的估计结果为 $B_{crit-oai} = E_{min}/S_{min}$，则当前的结果可写为：</p> \[B_{crit} = (1 + \beta)B_{opt} + \beta B_{crit-oai} \approx \beta B_{crit-oai},\] <p>两者之间近似只差一个倍数。这本质上反映的是两种方法在训练时间-训练数据上的权衡不同，具体来说，对于训练时间，我们有</p> \[S(B_{crit}) \approx (1 + \frac{1}{\beta}) S_{min}, \quad S(B_{crit-oai}) = 2S_{min}.\] <p>对于训练数据，代入 [1] 中的 ${E(B)} = E_{min} + B S_{min}$，我们也可以类似得到</p> \[E(B_{crit}) \approx (1 + \beta) E_{min}, \quad S(B_{crit-oai}) = 2 E_{min}.\] <p>所以两种方法本质上差不多，只不过是时间-训练的权衡有差别。具体到论文 [3] 中，$\beta=0.2$ 意味着更重视数据耗费，而不是训练时间。总而言之，这篇论文在 CBS 估计上的创新主要是确定目标 loss 的选取，以及通过消融定位来缓解调参的压力。</p> <h2 id="power-lines-scaling-laws-for-weight-decay-and-batch-size-in-llm-pre-training-4">Power Lines: Scaling Laws for Weight Decay and Batch Size in LLM Pre-training [4]</h2> <p>这篇文章的 CBS 方法基本也是 follow OpenAI 原论文 [2] 的方法的，但针对超参调整困难的问题进行了一些改良。从 [2] 中理论的原则上来说，为了达到目标 loss，对于不同 B，我们都应该把各个超参调到最优，但前文已经提到这是非常困难的，尤其是对于 lr schedule。[2] 中直接规避了这个问题，采用 const lr。[3] 中声称 const lr + EWA 可以实现常用的 lr schedule 的效果，所以采用 const lr + EWA 来降低调参的难度，但仍需要频繁地评测来判断是否达到了目标 loss。除此之外，在实际中，我们还是希望能够在真实使用的 lr schedule 上估计 CBS，[4] 这篇文章提出的方法就缓解了这个问题。</p> <p>首先，[4] 这篇文章把考虑范围收紧，假设实际中我们已经选好了一个 lr schedule，例如 10% warmup + 90% cosine decay。注意到，哪怕是在这种情况下，要得到 B 对应最优的训练步数/数据量也是不容易的，因为设置不同的步数就意味着不同的 lr schedule，没法复用已有的 trail。[4] 这篇文章就是缓解了这个问题。</p> <p>解决的方法也非常直接，就是尝试拟合一个 loss-token 的 scaling law。对于每个模型大小 N 和 bs B，使用确定好的 lr schedule，我们可以进行不同数据量 E 的训练，收集最后的 loss L，最后拟合一个如下的 scaling law：</p> \[L_{N,B}(E) = L_N + C_{N,B} E^{-\beta_{N,B}},\] <p>其中 $L_N$ 是对应大小模型的不可约 loss，第二项反映了数据越多，loss 越小。这个形式本质上就是 Chinchilla loss scaling law 的形式。此时给定一个目标 loss $L$，我们可以直接反解出对应的训练数据 $E(B)$ 和训练步数 $S(B)$：</p> \[E(B) = \left(\frac{C_{N,B}}{L-L_N}\right)^{\frac{1}{\beta_{N,B}}}, \quad S(B) = \frac{E(B)}{B}.\] <p>之后我们就可以利用之前提到的方法进行双曲线拟合，从而得到 CBS 的估计了。这篇文章也在不同的模型大小和数据量下估计了 CBS，结论和 [3] 类似：CBS 和模型大小关系不大，但是会随着训练数据量的增大而增大。</p> <h2 id="抛弃目标-loss">抛弃目标 loss</h2> <p>近期也有一些文章直接抛弃了“对齐目标 loss”这一条件，采用了更为简单直观，但没那么严谨的方式来估计给定模型大小和训练数据量时的 CBS。该方法就是用不同的 B 进行训练，然后找到 loss 开始明显变差的拐点，基本上可以总结为：</p> <ol> <li>固定模型大小和数据量，更改不同的 B 进行训练，收集最后的 loss；</li> <li>获得最优的 $B_{opt}$ 和对应的最优 loss $L(B_{opt})$；</li> <li>根据某种规则选择 $L(B)$ 的拐点确定为 $B_{crit}$。</li> </ol> <p>拐点的选择十分灵活。[5] 直接靠肉眼评估拐点；[6] 定义拐点为 $L(B_{crit}) - L(B_{opt}) = 0.01$，0.01 为一个主观的经验值；[7] 定义拐点为 $L(B_{crit}) = 1.0025 L(B_{opt})$。以 [6] 为例，示意图如下所示：</p> <div class="row justify-content-center"> <div class="col-12 col-md-10 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-08-cbs02/local_cbs-480.webp 480w,/assets/img/2026-08-cbs02/local_cbs-800.webp 800w,/assets/img/2026-08-cbs02/local_cbs-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2026-08-cbs02/local_cbs.jpeg" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="拐点确定，loss 与最优 loss 相差 0.01 以内" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <div class="caption"> 图 2：拐点确定，loss 与最优 loss 相差 0.01 以内。 </div> </div> </div> <p>可以看到，这种方法简单、易于理解，且和原方法是某种对偶关系。原来的方法是固定一个目标 loss，然后找到训练时间和数据耗费的权衡点；现在的方法本质上是固定训练数据，找到训练时间和 loss 的权衡点。相对来说，当前方法的理论支撑比原方法还是弱一些。未来工作可以适当考虑一下这一点。</p> <h2 id="总结">总结</h2> <p>这篇博客提出 OpenAI 原始 critical batch size（CBS）估计方法的两个问题，然后选取介绍了一些近年出现的更加方便实用的 CBS 估计方法。</p> <h2 id="references">References</h2> <ol> <li>经典回顾：OpenAI Critical Batch Size, https://chen-yu-zheng.github.io/blog/2026/CBS/</li> <li>An Empirical Model of Large-Batch Training, OpenAI, 2018</li> <li>How Does Critical Batch Size Scale in Pre-training?, ICLR, 2025</li> <li>Power Lines: Scaling Laws for Weight Decay and Batch Size in LLM Pre-training, NeurIPS, 2025</li> <li>The Design Space of Tri-Modal Masked Diffusion Models, 2026</li> <li>Critical Batch Size Revisited: A Simple Empirical Approach to Large-Batch Language Model Training, NeurIPS, 2025</li> <li>DeepSeek LLM Scaling Open-Source Language Models with Longtermism, 2024</li> </ol>]]></content><author><name></name></author><category term="optimization"/><category term="scaling"/><category term="batch-size"/><summary type="html"><![CDATA[针对 OpenAI 原始 CBS 论文的问题，选取介绍一些近年更加方便实用的 CBS 估计方法。]]></summary></entry><entry><title type="html">经典回顾：OpenAI Critical Batch Size</title><link href="https://chen-yu-zheng.github.io/blog/2026/CBS/" rel="alternate" type="text/html" title="经典回顾：OpenAI Critical Batch Size"/><published>2026-07-31T15:00:00+00:00</published><updated>2026-07-31T15:00:00+00:00</updated><id>https://chen-yu-zheng.github.io/blog/2026/CBS</id><content type="html" xml:base="https://chen-yu-zheng.github.io/blog/2026/CBS/"><![CDATA[<p>这篇博客对 OpenAI 在 critical batch size（CBS）方向上的经典工作 An Empirical Model of Large-Batch Training 做一个回顾，总结其核心的思想与方法。</p> <h2 id="cbs-是什么">CBS 是什么？</h2> <p>Batch size（B）是影响模型训练结果和效率的重要超参数，因此它的挑选特别重要。从经典随机优化的角度可以对其有一个直观的理解：</p> <ol> <li>当 B 很小的时候，梯度噪声很大，梯度估计很不准，此时学习率需要设置的比较小，而且要迭代相当多的步数才能达到预期的目标。在这种情况下，虽然对卡数的要求很小，但是需要训练相当长的时间，在实际中是不被期待的。</li> <li>当 B 在一定范围内成倍增大的时候，梯度噪声得到有效控制，我们可以从容地增大学习率，并期待能用成倍少的步数达到目标的效果。此时，我们能够充分利用卡的数量，把训练时间有效减少，同时保持较高的数据效率，这在实践中是比较可行的。</li> <li>当 B 非常大的时候，梯度估计更准确带来的增益几乎消失，我们没法再有效增大学习率，也不能期待进一步减少迭代步数。此时，我们需要花费更多的卡，但运行时间不能再有效减小，不仅算力耗费大，而且也是非常数据低效的。</li> </ol> <p>因此，选择不同的 B 对应着不同的时间-数据/计算的权衡，如下图所示：</p> <div class="row justify-content-center"> <div class="col-12 col-md-8 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-07-cbs/time_compute_tradeoff-480.webp 480w,/assets/img/2026-07-cbs/time_compute_tradeoff-800.webp 800w,/assets/img/2026-07-cbs/time_compute_tradeoff-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2026-07-cbs/time_compute_tradeoff.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="不同 bs 对应的时间-数据/计算的权衡" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <div class="caption"> 图 1：不同 bs 对应的时间-数据/计算的权衡。 </div> </div> </div> <p>CBS 正是一个反映第二阶段与第三阶段临界点的一个量。当 B 小于 CBS 时，我们视卡的数量，可以通过增大 B 来实现更高效的并行，缩短训练时间，保持较高的数据效率；而当 B 大于 CBS 时，进一步增大 B 难以进一步缩短训练时间，但会显著增加卡的数量和计算成本，数据效率降低。因此，CBS 能够科学地指导实际中 B 的选择，实现卡数和训练时间的高效分配，是一个重要的、值得估计的量。</p> <h2 id="单点二次模型与-cbs-信噪比估计量">单点二次模型与 CBS 信噪比估计量</h2> <p>从前面的初步分析可以看出，CBS 和梯度的信噪比存在深刻的关联。从这个角度出发，OpenAI 在原论文中，基于简单的二次模型，提出了一个 gradient noise scale 估计量，能够较为简单地估计 CBS 的数量级趋势，并且能够指导实际中 CBS 的经验拟合。这一节就对这部分的核心机制进行说明。</p> <p>从降低 loss 的角度来说，我们期待梯度下降（$B \to \infty$）的效果是最好的，但这需要及其多的卡数才能实现，对算力的要求过大。CBS 的核心就是找到一个 B，使得梯度噪声不大，估计仍然比较准确，此时达到目标性能的迭代步数和梯度下降差不多，但总体的算力耗费会少很多。因此，我们接下来在简单的单点二次模型上，先分析 GD（$B \to \infty$）的理想效果，然后研究 SGD（B）相较于 GD 的效果减幅，找到一个合适的 B 实现性能和算力的平衡。</p> <h3 id="gd-的最优学习率和理想效果">GD 的最优学习率和理想效果</h3> <p>我们目前只考虑训练过程中的某一个时刻（单点），设网络当前的权重为 $\theta$，对应的 loss function 为 $L(\theta) = \mathbb{E}_{x \sim \rho}[L_x(\theta)]$，其中 $x$ 为样本，$\rho$ 为对应的样本分布。为了实现 GD 场景下的理想效果，我们首先需要找到一个最优的学习率。由于神经网络的 $L(\theta)$ 本身非常复杂，所以为了简化分析，我们在临域内对 loss 采用二阶近似，且把这个叫做单点二次模型。设当前的梯度为 $g = \nabla L(\theta)$，当前的 Hessian 矩阵为$H = \nabla^2 L(\theta)$ ，那么当前点附近 loss 的二次近似可表示为：</p> \[L(\theta + \Delta \theta) \approx L(\theta) + g^\top \Delta\theta + \frac{1}{2} \Delta\theta^\top H \Delta\theta.\] <p>在 GD 的语境下，有 $\Delta\theta = -\eta g$，因此可得</p> \[L(\theta + \Delta \theta) \approx L(\theta) - \eta \|g\|_2^2 + \frac{\eta^2}{2} g^\top H g.\] <p>对 lr 求导，可以得到最优的 lr 为 $\eta_* = \frac{\Vert g\Vert_2^2}{g^\top Hg}$，对应的最优的 loss 下降幅度为</p> \[\Delta L_* = L(\theta) - L(\theta + \Delta \theta) \approx \eta_* \|g\|_2^2 - \frac{\eta_*^2}{2} g^\top H g = \frac{1}{2} \frac{\Vert g\Vert_2^4}{g^\top Hg}.\] <p>题外话，可以看到，最优 lr 和 loss 降幅都和 Hessian 矩阵 H 有着密切关系，现代的 adaptive optimizer 或多或少也有着近似 Hessian 二阶信息的思想存在。</p> <h3 id="sgd-的最优学习率和理想效果">SGD 的最优学习率和理想效果</h3> <p>在实际情况中，B 是有限的，我们只能使用 SGD 对模型进行优化。接下来我们就分析 SGD 对应的最优学习率和 loss 降幅，看看有限 B 带来的性能退化，并找到一个折中的平衡点。</p> <p>首先，此时我们不再能得到精确的梯度信息，而是一个近似的估计，为</p> \[\hat{g} = \frac{1}{B} \sum_{i=1}^B \nabla L_{x_i}(\theta), \quad x_i \sim \rho.\] <p>我们先看看随机梯度的统计性质，包括期望和协方差。期望上，我们可以得出随机梯度是真实梯度的一个无偏估计：</p> \[\mathbb{E}[\hat{g}] = \frac{1}{B} \sum_{i=1}^B \mathbb{E}_{x_i\sim \rho} [\nabla L_{x_i}(\theta)] = \frac{1}{B} \sum_{i=1}^B g(\theta) = g.\] <p>我们也可以类似计算其协方差：</p> \[\Sigma(\hat{g}) = \frac{1}{B^2} \sum_{i=1}^B \Sigma_{x_i \sim \rho}(\nabla L_{x_i}(\theta)) = \frac{1}{B^2} \sum_{i=1}^B \Sigma(\theta) = \frac{1}{B} \Sigma.\] <p>其中 $\Sigma$ 为样本梯度的协方差，为</p> \[\begin{aligned} \Sigma &amp;= \Sigma_{x \sim \rho}(\nabla L_{x}(\theta)) = \mathbb{E}_x[(\nabla L_{x}(\theta) - g)(\nabla L_{x}(\theta) - g)^\top] \\ &amp;= \mathbb{E}_x [\nabla L_{x}(\theta) \nabla L_{x}^\top(\theta)] - gg^\top. \end{aligned}\] <p>基于这些统计性质，我们现在就可以研究 SGD 的最优学习率和 loss 降幅了。和 GD 的情况不同，此时的梯度是随机的，所以我们更新后得到的 loss $L(\theta + \Delta \theta)$ 也是随机的。因此，我们研究其期望，并找到合适的学习率，使 loss 的期望下降最快。和之前类似，我们有</p> \[\begin{aligned} \mathbb{E}[L(\theta + \Delta \theta)] &amp;\approx L(\theta) + \mathbb{E}[g^\top \Delta\theta] + \frac{1}{2} \mathbb{E}[\Delta\theta^\top H \Delta\theta] \\ &amp;= L(\theta) - \eta \mathbb{E}[g^\top \hat{g}] + \frac{\eta^2}{2} \mathbb{E}[\hat{g}^\top H \hat{g}] \\ &amp;= L(\theta) - \eta \|g\|_2^2 + \frac{\eta^2}{2} \mathbb{E}[\hat{g}^\top H \hat{g}]. \end{aligned}\] <p>最后一项 $\mathbb{E}[\hat{g}^\top H \hat{g}]$ 的推导会稍微复杂一些：</p> \[\begin{aligned} \mathbb{E}[\hat{g}^\top H \hat{g}] &amp;= \mathbb{E}[tr(\hat{g}^\top H \hat{g})] = \mathbb{E}[tr(H \hat{g} \hat{g}^\top)] = tr(H \mathbb{E}[\hat{g} \hat{g}^\top]) \\ &amp;= tr(H (\Sigma(\hat{g}) + \mathbb{E}[\hat{g}] \mathbb{E}[\hat{g}^\top])) \\ &amp;= tr(H (\frac{\Sigma}{B} + {g}{g}^\top)) \\ &amp;= \frac{tr(H\Sigma)}{B} + {g}^\top H g. \end{aligned}\] <p>总结一下，更新一步之后的 loss 期望为：</p> \[\begin{aligned} \mathbb{E}[L(\theta + \Delta \theta)] \approx L(\theta) - \eta \|g\|_2^2 + \frac{\eta^2}{2} (\frac{tr(H\Sigma)}{B} + {g}^\top H g). \end{aligned}\] <p>对 lr 求导，可以得到 B 对应最优的 lr 为</p> \[\eta_*(B) = \frac{\Vert g\Vert_2^2}{g^\top Hg + \frac{1}{B} tr(H\Sigma)} = \frac{\eta_*}{1 + \frac{1}{B} \frac{tr(H\Sigma)}{g^\top Hg}}.\] <p>将其带入 loss 的表达式，可以得到 B 对应的最优 loss 降幅为</p> \[\Delta L_*(B) = L(\theta) - L(\theta + \Delta \theta) \approx \frac{\|g\|_2^4}{2 (g^\top Hg + \frac{1}{B} tr(H\Sigma))} = \frac{\Delta L_*}{1 + \frac{1}{B} \frac{tr(H\Sigma)}{g^\top Hg} }.\] <p>可以很明显看出，随着 B 的增大，$\eta_\ast(B)$ 和 $\Delta L_\ast(B)$ 都在增大，并逐渐趋近于 GD 对应的 $\eta_\ast$ 和 $\Delta L_\ast$ 。细致一看，可以发现，当 B 远小于 $\frac{tr(H\Sigma)}{g^\top Hg}$ 时，增大 B 近似对应着最优学习率和 loss 降幅的线性增加。此时，我们可以线性增加学习率，并且有效（线性）减小训练步数，这对应着我们最开始提到的阶段二。当 B 远大于 $\frac{tr(H\Sigma)}{g^\top Hg}$ 时，再增加 B 带来的 loss 降幅收益几乎消失，训练时间不会再明显减少，反而需要更多的卡，这对应着我们最开始提到的阶段三。两阶段的临界点自然引出了 CBS 的估计量，如下图所示：</p> <div class="row justify-content-center"> <div class="col-12 col-md-10 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-07-cbs/lr_bs-480.webp 480w,/assets/img/2026-07-cbs/lr_bs-800.webp 800w,/assets/img/2026-07-cbs/lr_bs-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2026-07-cbs/lr_bs.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="最优学习率-B 关系图, loss 降幅也类似" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <div class="caption"> 图 2：最优学习率-B 关系图, loss 降幅也类似。 </div> </div> </div> <h3 id="local-cbs-的估计量gradient-noise-scale">Local CBS 的估计量：Gradient Noise Scale</h3> <p>从上一节可以看出，阶段二和阶段三的临界点发生在 $\frac{tr(H\Sigma)}{g^\top Hg}$ 附近，我们将其叫做 Gradient Noise Scale，并明确定义为</p> \[B_{noise} = \frac{tr(H\Sigma)}{g^\top Hg}.\] <p>可以看到其分子部分反映的是梯度的噪声水平（按照曲率大小加权，曲率越大权重越大），分母部分反映的是梯度的信号水平。当 loss 较低或者任务很复杂的时候，信噪比较低，$B_{noise}$ 比较大，更适用于使用大 B 来更准地估计梯度，加速训练。反之，用大 B 不能带来明显的训练加速，反而会造成算力和数据的浪费。这和我们开始对 CBS 的感知非常一致，原论文正是用这个量来估计 local 的数量级，并预测其走势。</p> <p>在实际的神经网络训练中，Hessian 矩阵通常是比较难计算的，所以文章又进一步假设 Hessian 阵为单位阵（各向同性），提出了一个简化版的 local CBS 估计量，适用于平时的实际估计：</p> \[B_{simple} = \frac{tr(\Sigma)}{\|g\|^2_2}.\] <p>在实际中，可以在训练的不同时间点估计该统计量，来判断当前的 local CBS，并视情况动态调节 B。</p> <h2 id="全程推广与时间-算力权衡">全程推广与时间-算力权衡</h2> <p>上一节，我们考虑并分析了训练过程中单点附近 GD/SGD 的理论性质，并提出了单点附近 local CBS 的估计量。现在，我们将思想拓展到训练的全程，并回答一个问题：为了训练到同一个目标 loss，不同 B 分别需要多少优化步、处理多少数据（或花费多少算力）？文章最终得到一个关于训练时间-数据/计算效率的双曲线 Pareto frontier，并基于此提出了 global CBS 的估计量。</p> <p>我们记 $B_{noise}(s)$ 为 s 时刻对应的 gradient noise scale。记 S 为达到目标 loss 的优化步数，其衡量了串行时间效率。记 E 为达到目标 loss 的所需数据量，其衡量了数据效率或者计算成本。<strong>我们也假设每一步的学习率都已经按上一节调至最优</strong>。</p> <h3 id="小试牛刀gradient-noise-scale-不变">小试牛刀：Gradient Noise Scale 不变</h3> <p>我们先考虑最简单的情况，假设训练全程 $B_{noise}(s)$ 都不变，恒为 $B_{noise}$。当 bs 为 B 时，从上一节的推导我们可以知道，当参数处于同一个点时，SGD 每一步的 loss 降幅与 GD 对应降幅的比值为</p> \[\frac{\Delta L_*(B)}{\Delta L_*} \approx \frac{1}{1 + B_{noise}/B}.\] <p>如果进一步假设不同 B 的训练可以沿同一条有效进度轨迹对齐，且该比例能够沿训练过程累积，我们可以近似认为 GD 一步得到 loss 降幅需要 SGD $1 + B_{noise}/B$ 步来实现。设 GD 达到目标 loss 的步数为 $S_{min}$（从当前推导思想出发，GD 是最快的），则 SGD 大致需要 $S(B) = S_{min}(1 + B_{noise}/B)$ 步，对应数据量为 $E(B) = S_{min}(B + B_{noise})$。从这里出发，我们可以得到时间 S 和数据 E 的双曲线 Pareto frontier。</p> <p>我们先考虑时间和数据两种效率的极端情况，即双曲线的两侧渐近线。为了让训练时间 $S(B)$ 最短，我们需要让 B 趋近于无穷，此时步数趋近于 $S(\infty) = S_{min}$，但数据/计算的耗费是无穷大的。为了让数据/计算耗费最小，我们需要让 $B=1$，此时数据耗费为 $E_{min} = E(1) = S_{min}(1 + B_{noise}) \approx S_{min} B_{noise}$，但训练步数也是最长的。</p> <p>我们再考虑一般的情况。基于 $S_{min}$ 和 $E_{min} \approx S_{min} B_{noise}$ 的定义，我们可以得到 S 与 E 的双曲线关系：</p> \[\begin{aligned} &amp; \frac{S(B)}{S_{min}} - 1 = \frac{B_{noise}}{B}, \\ &amp; \frac{E(B)}{E_{min}} - 1 \approx \frac{B}{B_{noise}}, \\ &amp; \implies \frac{E(B)}{E_{min}} - 1 \approx \left(\frac{S(B)}{S_{min}} - 1\right)^{-1}. \end{aligned}\] <p>在实际中，我们可以在不同 B 训练的时候收集数据，并拟合出 $S_{min}$、$E_{min}$ 及其对应的双曲线。如下图所示：</p> <div class="row justify-content-center"> <div class="col-12 col-md-6 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-07-cbs/SE_curve-480.webp 480w,/assets/img/2026-07-cbs/SE_curve-800.webp 800w,/assets/img/2026-07-cbs/SE_curve-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2026-07-cbs/SE_curve.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="实际的训练时间-数据耗费双曲线" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <div class="caption"> 图 3：实际的训练时间-数据耗费双曲线，符合理论。 </div> </div> </div> <p>最后，我们看看我们提出的 local CBS 的估计量 $B_{noise}$ 在时间-数据权衡的视角里意味着什么。一，一旦我们完成双曲线的拟合，我们就可以得到</p> \[B_{noise} \approx E_{min}/S_{min},\] <p>这给了 Gradient Noise Scale 之外的另一种估计方法，在实际中也是更准的一种选择 CBS 的方法。</p> <p>二，将 $B_{noise}$ 带入 S 和 E 的表达式，我们可以得到</p> \[S(B_{noise}) = 2S_{min}, \quad E(B_{noise}) \approx 2E_{min},\] <p>这意味着 $B_{noise}$ 在时间和数据效率的两种极限之间取了一个折中之处。自然地，原论文把这种情况的 global CBS 估计量就定义为：</p> \[B_{crit} = \frac{E_{min}}{S_{min}} \approx B_{noise}.\] <h3 id="继续深入gradient-noise-scale-变化">继续深入：Gradient Noise Scale 变化</h3> <p>实际的训练过程中，$B_{noise}$ 是不断变化的（先小后大），所以上面的 global CBS 估计方法并不一定成立。我们将上一小节的思想继续推广至 $B_{noise}(s)$ 随时间变化的场景，推导原则是一样的。我们仍然假设不同 B 的训练可以沿同一条有效进度轨迹对齐，且该比例能够沿训练过程累积。以 GD 的训练时间为基准，设在时间 $s$ 处，SGD 的 bs 为 $B(s)$，那么为了完成 GD 此时 $ds$ 时间的 loss 降幅，SGD 近似需要 $(1 + B_{noise}(s)/B(s))ds$ 的时间来实现。和之前推导类似，我们可以得出，为了达到目标效果，SGD 需要的时间和数据量分别为：</p> \[\begin{aligned} &amp; S(B(s)) = \int (1 + \frac{B_{noise}(s)}{B(s)})ds, \\ &amp; E(B(s)) = \int B(s)(1 + \frac{B_{noise}(s)}{B(s)})ds = \int (B(s) + {B_{noise}(s)})ds. \end{aligned}\] <p>和上一小节类似，我们也先考虑时间和数据两种效率的极端情况。为了让训练时间 S 最小，我们仍然是需要 $B(s) = \infty$，此时有 $S_{min} = \int ds$，即 GD 所需要的时间。为了让训练数据 E 最小，我们仍然是需要 $B(s)=1$，此时 $E_{min} = \int (1 + {B_{noise}(s)})ds \approx \int B_{noise}(s)ds$。</p> <p>为了分析简便，我们接下来暂时假设 SGD 的 bs 不随时间变化，即 $B(s) = B$，这也是实际中的默认情况。我们看到，此时上一节的双曲线规律仍然成立。具体来说，我们有：</p> \[\begin{aligned} &amp; \frac{S(B)}{S_{min}} - 1 = \frac{1}{B} \frac{\int B_{noise}(s) ds}{S_{min}} \approx \frac{1}{B} \frac{E_{min}}{S_{min}}, \\ &amp; \frac{E(B)}{E_{min}} - 1 \approx B \frac{\int ds}{E_{min}} = B \frac{S_{min}}{E_{min}}, \\ &amp; \implies \frac{E(B)}{E_{min}} - 1 \approx \left(\frac{S(B)}{S_{min}} - 1\right)^{-1}. \end{aligned}\] <p>所以，如果我们用随时间恒定的 bs schedule 来训练的话，通过改变不同的 B 收集训练数据，实际中仍然能拟合到和上一节一样的的双曲线性质。</p> <p>现在，和上一节一样，我们也可以类似定义 CBS 的估计量，即</p> \[B_{crit} = \frac{E_{min}}{S_{min}} \approx \frac{\int B_{noise}(s)ds}{\int ds}.\] <p>不难看出，这就是 gradient noise scale 在时间上的平均。这意味着，如果用随时间恒定的 B schedule 来训练的话，CBS 是可以用 gradient noise scale 在训练过程上的平均来反映的。事实上，和上一节一样，我们有 $S(B_{crit}) = 2S_{min},\ E(B_{crit}) \approx 2E_{min}$，这意味着 $B_{crit}$ 在时间和数据效率的两种极限之间取了一个折中之处。</p> <h3 id="batch-size-schedule">Batch Size Schedule</h3> <p>在上一节，我们通过假设 $B(s) = B$，得到了时间 S 和数据 E 的双曲线函数，并提出了基于双曲线拟合和 gradient noise scale 的 global CBS 估计。不过，这看起来并不是一个最优的选项。既然 $B_{noise}$ 是随时间变化的，那我们为什么不随时间动态调整我们的 B 呢？例如，在训练初期，梯度信号比较大，信噪比较高，$B_{noise}$ 比较低，我们可以用比较小的 B 来实现有效的 loss 下降；在训练后期，梯度信号比较少，信噪比较低，$B_{noise}$ 比较高，我们可以用比较大的 B 来实现有效的 loss 下降；这样子看起来无论是时间还是数据效率都是更优的。在这一节，我们就做一个理论上的探讨。</p> <p>首先把问题 formulate 一下，我们希望找到一个合理的 bs schedule $B(s)$，尽可能地减小训练时间 S 和数据总量 E，即</p> \[\min_{B(s)} S(B(s)) + \frac{1}{\lambda} E(B(s)) = \min_{B(s)} \int (1 + \frac{B_{noise}(s)}{B(s)})ds + \frac{1}{\lambda} \int (B(s) + {B_{noise}(s)})ds,\] <p>其中 $\lambda$ 表示的是训练时间与数据耗费的权衡，为一个自由变量。把问题中与 $B(s)$ 无关的项去掉，我们也可以等价地转化为研究</p> \[\min_{B(s)} \int \frac{B_{noise}(s)}{B(s)}ds + \frac{1}{\lambda} \int B(s)ds.\] <p>为了得到最优的 bs schedule，我们对 $B(s)$ 做变分，并令其为 0，可以得到</p> \[- \frac{B_{noise}(s)}{B^2(s)} + \frac{1}{\lambda} = 0.\] <p>这轻松导出了我们最优的 bs schedule：</p> \[B(s) = \sqrt{\lambda B_{noise}(s)}.\] <p>其实这里和直观猜测有些不同，直观感觉既然 $B_{noise}(s)$ 是 local CBS 的一个良好估计，那就直接让 $B(s) = B_{noise}(s)$ 就行了。但理论告诉我们，从全局的考量来看，更优的选择应该是根号阶数的。现在，我们将 bs schedule 代回 S 和 E 的表达式，并看看有没有类似先前的双曲线规律。我们有训练时间为</p> \[S(B(s)) = \int (1 + \frac{B_{noise}(s)}{B(s)})ds = S_{min} + \frac{\int \sqrt{B_{noise}(s)} ds}{\sqrt{\lambda}} := S_{min} + \frac{\Phi}{\sqrt{\lambda}},\] <p>其中 $\Phi = \int \sqrt{B_{noise}(s)} ds$。我们也可以得到数据耗费为</p> \[E(B(s)) = \int (B(s) + {B_{noise}(s)})ds \approx E_{min} + \sqrt{\lambda} \Phi.\] <p>可以推得和前文类似的双曲线规律：</p> \[\frac{E(B(s))}{E_{min}} - 1 \approx \frac{\Phi^2}{E_{min} S_{min}} \left(\frac{S(B(s))}{S_{min}} - 1\right)^{-1} := \gamma \left(\frac{S(B(s))}{S_{min}} - 1\right)^{-1},\] <p>其中 $\gamma = {\Phi^2}/{E_{min} S_{min}} = (\int \sqrt{B_{noise}(s)} ds)^2 / E_{min} S_{min}$。由于使用 bs schedule 进行训练的时间和数据耗费理论上肯定不会差于固定 bs，我们猜测 $\gamma \leq 1$。事实上，这是成立的。基于柯西不等式，我们有：</p> \[\gamma = \frac{(\int \sqrt{B_{noise}(s)} ds)^2}{E_{min} S_{min}} \leq \frac{(\int {B_{noise}(s)} ds) (\int ds) }{E_{min} S_{min}} = \frac{E_{min} S_{min}}{E_{min} S_{min}} = 1,\] <p>当 $B_{noise}(s)$ 不随时间变化时取等，此时回退到之前的情况，采用 bs schedule 并不能带来效率的提升。而对于一般的 $B_{noise}(s)$ 随时间变化的情况有 $\gamma &lt; 1$，这意味着引入 bs schedule 对应的双曲线更低，有着更好的时间和数据效率。一般来说，gradient noise scale 变化越剧烈，$\gamma$ 通常越小，引入 bs schedule 的理论收益越大。如下图所示，用了 bs schedule 的点都落在 const bs 的曲线内部，说明效率更高。</p> <div class="row justify-content-center"> <div class="col-12 col-md-10 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-07-cbs/bs_schedule-480.webp 480w,/assets/img/2026-07-cbs/bs_schedule-800.webp 800w,/assets/img/2026-07-cbs/bs_schedule-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2026-07-cbs/bs_schedule.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="bs schedule vs. const bs 的双曲线" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <div class="caption"> 图 4：bs schedule vs. const bs 的双曲线，bs schedule 更好。 </div> </div> </div> <p>对于权衡系数 $\lambda$，原文也给出了一个自然但非唯一的选择：</p> \[\lambda = B_{crit} = \frac{E_{min}}{S_{min}}.\] <p>此时对应的训练时间为：</p> \[S(B(s)) = S_{min} + \frac{\Phi}{\sqrt{\lambda}} = S_{min} + \Phi \frac{\sqrt{S_{min}}}{\sqrt{E_{min}}} = S_{min}(1 + \frac{\Phi}{\sqrt{S_{min} E_{min}}}) = S_{min}(1 + \sqrt{\gamma}),\] <p>对应的样本耗费为：</p> \[E(B(s)) \approx E_{min} + \sqrt{\lambda} \Phi = E_{min}(1 + \sqrt{\gamma}).\] <p>和之前类似，这个选择相对于纯时间最优和纯数据最优的极限恶化相同的倍数，达到了某种平衡折中的阶段。</p> <h2 id="cbs-的经验估计">CBS 的经验估计</h2> <p>最后，我们基于前面的理论，总结一下实际中 CBS 的估计方法。一种是基于 gradient noise scale 的，一种是基于拟合双曲线的。我们默认不考虑 bs schedule 的情况。</p> <h3 id="基于-gradient-noise-scale-的-cbs-估计">基于 gradient noise scale 的 CBS 估计</h3> <p>回顾前面分析训练全程的理论，我们已知 $B_{crit} \approx \frac{\int B_{noise}(s)ds}{\int ds}$ 是 global CBS 的一个比较好的估计量，所以在实际中可以跑一次基准训练，在不同时间估计一下 $B_{noise}(s)$ 或者计算更简单的 $B_{simple}(s)$，然后取平均就可以。由于我们的理论是基于最优学习率的，所以估计 CBS 的基准训练的学习率等超参也需要比较好。如文章提到了学习率太小会抬高 noise scale 的估计。实际中可以用公认比较安全的超参进行实验。</p> <p>在实践中，这个方法计算比较简单，也可以比较好地估计真实 CBS 的数量级和趋势，但距离精准预测 CBS 还有较大的差距，所以不太适合精确确定 bs 的场景。</p> <h3 id="基于拟合双曲线的估计">基于拟合双曲线的估计</h3> <p>从前面的分析中，我们也知道，可以先拟合时间-数据双曲线，然后再用 $B_{crit} = \frac{E_{min}}{S_{min}}$ 来获得 CBS 的估计。具体的实操流程为：</p> <ol> <li>先固定目标性能。我们前面的分析都是假设了达到某个目标 loss 的。CBS 一般是和 loss 相关的，loss 越小，梯度信号通常越小，CBS 往往越大。因此，我们需要先固定一个目标性能。</li> <li>扫描不同 bs，并对不同的 bs 扫描学习率等超参。</li> <li>对于每个 batch size，选择最快达到目标性能的 run，记录其训练步数 S 和数据耗费 E。</li> <li>拟合时间-数据双曲线，然后用 $B_{crit} = \frac{E_{min}}{S_{min}}$ 来获得 CBS 的估计。</li> </ol> <p>在实践中，这个方法计算成本更高，但能够更为精准地预测 CBS。两种方法的对比如下图所示：</p> <div class="row justify-content-center"> <div class="col-12 col-md-6 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2026-07-cbs/cbs_est-480.webp 480w,/assets/img/2026-07-cbs/cbs_est-800.webp 800w,/assets/img/2026-07-cbs/cbs_est-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2026-07-cbs/cbs_est.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="不同的 CBS 估计方法对比" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <div class="caption"> 图 5：不同的 CBS 估计方法对比。 </div> </div> </div> <h2 id="总结">总结</h2> <p>本文对 OpenAI 在 CBS 上的经典工作 An Empirical Model of Large-Batch Training 做了一个回顾，总结其核心的理论思想与对应的 CBS 估计方法。</p>]]></content><author><name></name></author><category term="optimization"/><category term="scaling"/><category term="batch-size"/><summary type="html"><![CDATA[回顾总结 OpenAI 的 CBS 经典工作 An Empirical Model of Large-Batch Training。]]></summary></entry><entry><title type="html">muP 漫游 (一)：muP 是什么？我们为何需要 muP？</title><link href="https://chen-yu-zheng.github.io/blog/2026/muP-01/" rel="alternate" type="text/html" title="muP 漫游 (一)：muP 是什么？我们为何需要 muP？"/><published>2026-06-16T03:57:00+00:00</published><updated>2026-06-16T03:57:00+00:00</updated><id>https://chen-yu-zheng.github.io/blog/2026/muP-01</id><content type="html" xml:base="https://chen-yu-zheng.github.io/blog/2026/muP-01/"><![CDATA[<p>Maximal Update Parameterization（muP）不仅是一套实现大模型稳定与高效训练的理论框架，更已成为现代预训练范式的底层思想之一：从超参数跨尺度迁移的规律，到优化器的设计原则，乃至网络架构本身的演进，处处可见其深刻影响。在过去一年多 muP 及其相关领域的学习中，笔者时常感慨于这套思想的简洁与力量，也愈发希望国内大模型社区能更清晰地理解、拥抱 muP。因此，本系列博客试图从基础理论到前沿应用，对 muP 做一个完整而详细的介绍。</p> <p>在引入 muP 的具体概念之前，我们首先得明确它究竟在试图解决什么问题。我们的漫游之旅，就从大模型训练中的两大根本难题说起。</p> <h2 id="大模型训练的两大难题">大模型训练的两大难题</h2> <p>众所周知，当神经网络变得越来越大的时候，训练也会变得越来越难。在这个系列，我们具体讨论其中的两个较为本质的问题，<strong>一是神经网络的特征学习能力渐进失效，二是大模型调参困难，且超参难以从小模型迁移到大模型</strong>。</p> <p><strong>一，神经网络的特征学习能力渐进失效。</strong>能够自适应从数据中学到有效的特征（各层的输出/激活值），是深度学习超越传统机器学习算法（如核方法）并取得巨大成功的本质原因之一。然而，先前的理论和实践研究都表明，当神经网络的大小（如宽度和深度）趋于无穷时，其特征学习能力往往会逐渐趋于失效。具体表现为神经网络各层特征的初始化和训练更新量会逐渐趋于无穷（爆炸）或者零（冻结），从而导致大模型的训练处于不稳定或者非常低效的场景。因此，如何保证神经网络在渐进情况下实现高效的特征学习是一个重要的问题。</p> <p><strong>二，大模型调参困难，且超参难以从小模型迁移到大模型。</strong>毫无疑问地，超参数（初始化方差、学习率、weight decay 等）对神经网络训练的效果有着决定性的影响。当模型比较小的时候，我们有能力用网格搜索、随机搜索等方法进行充分的调参试错。然而，当模型非常大的时候（如上百 B 甚至上 T 参数），普通的调参试错带来的运行成本往往是难以令人接受的。这时，一个很自然的想法是能不能将小模型上的超参直接复用到大模型的训练中？答案也是否定的，这样的操作往往会带来较为严重的训练不稳定。因此，如何科学、经济地获得大模型训练的超参，也是一个重要的问题。</p> <p>对于问题二，我们还可以额外讨论一下 scaling law 的思路。经典主流的 scaling law 往往预测的是模型在目标规模上能达到的最优性能，但对于如何找到最优性能对应的超参并没有做出明确的解答。近两年也陆陆续续有一些 hyperparameter scaling law 的工作出现，但笔者感觉，它们的结论往往依赖于特定的任务、模型和数据，能否泛化至一般的训练场景仍然未知。Hyperparameter scaling law 的上限还不太明朗，因此本系列不做额外讨论。我们现在先默认问题二没有一个好的、通用的解决方案。</p> <p><strong>muP 的横空出世，很大程度上改变了我们面对这两大难题的无力局面。理论上</strong>，muP 从特征学习稳定高效的原则出发，对问题一做出了科学的解答（当然是有一些假设的，我们后续会提到），能够保证规模扩展时模型特征学习的稳定与高效。<strong>经验上</strong>，muP 在实际落地中展现了强大的指导超参数跨规模迁移的能力，使得小模型上搜得的超参可以被科学地复用到大模型上，实现大规模训练优异的性能。因此，<strong>muP 毫无疑问是大模型时代一个非常重要的理论成果（向 Greg Yang 大佬致敬）</strong>，我们后续的博客会逐步展开，详细介绍。</p> <p>说了这么多文字，读者也不一定能准确地理解到每个细节。接下来，我们就从简单的理论 toy model 来直接感受一下大模型训练的两大难题。</p> <h2 id="从两层线性神经网络看大模型的训练难题">从两层线性神经网络看大模型的训练难题</h2> <p>我们尽可能地用最简单的例子来理解大模型训练的两大难题。数据上，我们假设只有一个数值正常的一维样本 $(x,y)$，即 $x, y = \Theta(1) \in \mathbb{R}$。模型上，我们的神经网络模型定义为两层 linear MLP，为 $f(x) = w_2^\top w_1 x$，参数维度为 $w_1, w_2 \in \mathbb{R}^n$。我们此时考虑的“大模型”，就是模型宽度 $n$ 非常大的两层 MLP。目标方面，我们定义 loss function 为 $L(f(x),y)$。优化算法上，为了简单，我们也只考虑一步梯度下降，并合理假设此时 $\partial L/\partial f = \Theta(1)$。</p> <p>超参数的选择对神经网络的训练有着至关重要的影响，为了更好地显现出现实情况存在这两大难题，我们这里分析标准的超参参数化（Standard Parameterization/SP）。在这个简单的模型上，超参包括参数的初始化 $w_{ij} \sim \mathcal{N}(0, \sigma_i^2)$ 和梯度下降的学习率 $\eta$。对于初始化方差，我们使用实际中常用的类 Kaiming Normalization，为 $\sigma_1^2 = 1, \sigma_2^2 = 1/n$。学习率我们暂定为一个待定的数。</p> <p>为了展现出前文所提的大模型的训练难题，我们现在需要对网络初始化和更新一步后的特征（features）进行分析。特征的具体定义就是模型每一层的输出/激活值。比如在我们这个 toy 模型中，我们可以记特征向量为 $h_1(x) = w_1 x$ 和 $h_2(x) = w_2^\top h_1(x) = f(x)$。我们用 RMS norm 来衡量特征向量的平均大小，即对于 $h \in \mathbb{R}^d$，定义 $\Vert h\Vert_{R} = \sqrt{\frac{\sum_i h_i^2}{d}}$。</p> <h3 id="前传特征分析">前传特征分析</h3> <p>众所周知，对于普通 MLP，在类 Kaiming Normalization 的作用下，即使网络非常宽，前传特征也是可以保持稳定的，这保证了网络有一个健康不会爆炸的初始化。我们在这里还是做一下简单的分析。对于第一层输出的特征向量 $h_1(x) = w_1 x$，显然各维是独立同分布的零均值高斯分布，且 scale 满足</p> \[\mathbb{E} [h_1(x)_i^2] = \mathbb{E}[w_{1i}^2] x^2 = \sigma_1^2 x^2 = x^2 = \Theta(1) \implies \Vert h_1(x)\Vert_{R} = \Theta(1).\] <p>所以第一层输出的特征 $h_1(x)$ 是稳定的。对于网络（第二层）的输出 $f(x)$，也是零均值的，scale 也有类似的推导：</p> \[\begin{aligned} \mathbb{E}[f(x)^2] &amp;= \mathbb{E}\left[\left(\sum_{i=1}^n w_{2i} h_{1i}\right)^2\right] = \mathbb{E}\left[\sum_{i,j=1}^n w_{2i} w_{2j} h_{1i} h_{1j}\right] \\ &amp;= \sum_{i=1}^n \mathbb{E}[w_{2i}^2] \mathbb{E}[h_{1i}^2] = \sum_{i=1}^n \sigma_2^2 \mathbb{E}[h_{1i}^2] \\ &amp;= \sum_{i=1}^n \frac{1}{n} \mathbb{E}[h_{1i}^2] = x^2 = \Theta(1) \\ &amp; \implies \Vert f(x)\Vert_{R} = |f(x)|= \Theta(1). \end{aligned}\] <p>因此，在类 Kaiming Normalization 的作用下，即使两层 MLP 的宽度非常大，其初始化时的前传特征也是稳定的。</p> <h3 id="梯度分析">梯度分析</h3> <p>我们接下来的目标是一步梯度下降后的特征更新量，为了得到这个量，我们得先计算权重的变化量，这又需要我们先计算梯度。直接利用链式法则计算可以得到各梯度（一个简单的入门教程：<a href="https://www.cnblogs.com/pinard/tag/%E7%9F%A9%E9%98%B5%E6%B1%82%E5%AF%BC%20%20%E5%90%91%E9%87%8F%E6%B1%82%E5%AF%BC/">博客园-矩阵向量求导</a>）：</p> \[\begin{aligned} &amp; \frac{\partial L}{\partial w_2} = \frac{\partial L}{\partial f} \frac{\partial f}{\partial w_2} = \frac{\partial L}{\partial f} w_1 x = \frac{\partial L}{\partial f} h_1, \\ &amp; \frac{\partial L}{\partial h_1} = \frac{\partial L}{\partial f} \frac{\partial f}{\partial h_1} = \frac{\partial L}{\partial f} w_2, \\ &amp; \frac{\partial L}{\partial w_1} = \frac{\partial L}{\partial f} \frac{\partial h_1^\top}{\partial w_1} \frac{\partial f}{\partial h_1} = \frac{\partial L}{\partial f} w_2 x. \end{aligned}\] <h3 id="更新特征分析问题一">更新特征分析（问题一）</h3> <p>我们现在可以分析特征在一步梯度下降中的更新量了。首先看第一层的特征更新 $\Delta h_1(x)$（因为我们假设了只有一个数据，所以第二次前传的数据并不变化），我们有：</p> \[\Delta h_{1} = \Delta w_{1} x = -\eta \frac{\partial L}{\partial w_{1}} x = -\eta \frac{\partial L}{\partial f} w_{2} x^2 = -\Theta(\eta w_{2})\] <p>我们上面的推导用了前文假设的 loss function 对输出的梯度是常数级的（$\partial L/\partial f = \Theta(1)$），<strong>这在训练初期是一个合理的假设</strong>。我们也不太严谨地对向量用了 $\Theta$ 符号，不过应该不影响理解。基于此，我们可以进一步看 $\Delta h_1(x)$ 的 scale：</p> \[\begin{aligned} \mathbb{E}[\Delta h_{1i}^2] &amp;= \Theta(\mathbb{E}[\eta^2 w_{2i}^2]) = \Theta(\mathbb{E}[\eta^2 \sigma_{2}^2]) = \Theta(\frac{\eta^2}{n}) \\ &amp; \implies \Vert \Delta h_1(x)\Vert_R = \Theta(\frac{\eta}{\sqrt{n}}). \end{aligned}\] <p><strong>若学习率不随网络宽度变化（$\eta = \Theta(1)$），这个理论结果意味着当模型逐渐变大时（$n \to \infty$），至少在训练初期，第一层的特征更新会趋近于 0（$\Delta h_{1} \to 0$）。此时，模型的特征学习能力渐进消失，正反映了我们上面所述的问题一。</strong></p> <p>类似地，我们也可以进一步估计 $\Delta f(x)$ 的 scale，首先化简其表达形式：</p> \[\Delta f(x) = \Delta w_{2}^\top (h_1 + \Delta h_1) + w_{2}^\top \Delta h_1,\] <p>其中 $\Delta w_2 = - \eta \frac{\partial L}{\partial w_2} = - \eta \frac{\partial L}{\partial f} h_1$。我们接下来分别估计每一项的 scale。首先，对于 $\Delta w_{2}^\top h_1$，我们有</p> \[\begin{aligned} &amp;\Delta w_{2}^\top h_1 = - \eta \frac{\partial L}{\partial f} h_1^\top h_1, \\ &amp; \mathbb{E} [\Delta w_{2}^\top h_1] = \Theta(\mathbb{E}[\eta h_1^\top h_1]) = \Theta(\eta \sum_{i=1}^n \mathbb{E}[h_{1i}^2]) = \Theta(\eta n x^2) = \Theta(\eta n). \end{aligned}\] <p>对于第二项 $\Delta w_{2}^\top \Delta h_1$，我们有：</p> \[\begin{aligned} &amp;\Delta w_{2}^\top \Delta h_1 = - \eta \frac{\partial L}{\partial f} h_1^\top \cdot -\eta \frac{\partial L}{\partial f} w_{2} x^2 = \eta^2 (\frac{\partial L}{\partial f})^2 x^2 w_2^\top h_1 = \eta^2 (\frac{\partial L}{\partial f})^2 x^2 f(x), \\ &amp; \mathbb{E} [|\Delta w_{2}^\top \Delta h_1|] = \eta^2 (\frac{\partial L}{\partial f})^2 x^2 \mathbb{E}[|f(x)|] = \Theta(\eta^2). \end{aligned}\] <p>对于第三项 $w_{2}^\top \Delta h_1$，我们有</p> \[\begin{aligned} &amp; w_{2}^\top \Delta h_1 = w_{2}^\top \cdot -\eta \frac{\partial L}{\partial f} w_{2} x^2 = -\eta \frac{\partial L}{\partial f} x^2 w_2^\top w_2, \\ &amp; \mathbb{E}[w_{2}^\top \Delta h_1] = \Theta(\eta \mathbb{E}[w_2^\top w_2]) = \Theta(\eta \sum_{i=1}^n \mathbb{E}[w_{2i}^2]) = \Theta(\eta \cdot n \frac{1}{n}) = \Theta(\eta). \end{aligned}\] <p>综合上面的结果，我们可以估计 $\Delta f(x)$ 的 scale，为</p> \[|\Delta f(x)| = \Theta(\eta n) + \Theta(\eta^2) + \Theta(\eta).\] <p><strong>可以看到，若学习率不随网络宽度变化（$\eta = \Theta(1)$），当模型逐渐变大时（$n \to \infty$），网络输出的更新量会趋近于无穷（$\Delta f(x) = \Theta(n)$）。此时，模型的特征学习能力渐进爆炸，无法稳定训练，也反映了我们上面所述的问题一。</strong></p> <p>一个自然的补救想法是：既然 $\Delta f(x)$ 的主导项是 $\Theta(\eta n)$，那我们是否可以令 $\eta = \Theta(1/n)$，从而让输出更新保持在常数级？这确实可以避免 $\Delta f(x)$ 爆炸，但代价是第一层特征更新会进一步缩小：</p> \[\Vert \Delta h_1(x)\Vert_R = \Theta\left(\frac{\eta}{\sqrt n}\right) = \Theta\left(\frac{1}{n^{3/2}}\right).\] <p>因此，在实际的标准参数化（SP）上，仅仅寻找一个随宽度缩放的全局学习率并不能真正解决问题：若 $\eta=\Theta(1)$，输出更新爆炸；若 $\eta=\Theta(1/n)$，输出更新稳定但第一层特征冻结得更加彻底。换句话说，<strong>SP 不存在一个简单的全局超参数（学习率）缩放，可以同时让每一层的特征更新都处在合适的尺度，这意味着模型的特征学习能力往往会规模极限失效</strong>。</p> <h3 id="超参难以迁移问题二">超参难以迁移（问题二）</h3> <p>我们现在接着来说明，<strong>超参数（如学习率）是无法从小模型上搜索然后直接迁移到大模型上的。</strong> 这种方式意味着超参设置与模型大小无关，在数学上就表示为 $\eta = \Theta(1)$。从上面的理论结果我们可以直接得到，第一层的特征更新会趋向于冻结，而输出层的特征更新反而会趋向于爆炸，是一个非常病态的学习状态，导致大模型的训练效果会非常差。这意味着，小模型上最优的超参不能直接复用到大模型训练上。另一方面，在大模型上进行试错性调参，又是非常困难，耗费巨大的。两者结合，对应了我们上面所提的问题二。</p> <h2 id="mup-的解决思路">muP 的解决思路</h2> <p>从上面的推导可以看出，SP 存在两个问题：（1）超参无法直接从小模型复用到大模型（2）全局的超参（学习率）设置导致也无法通过调参来实现各层特征同时的稳定更新。但是，我们看起来还是有机会通过随模型大小，分层、科学地调整超参来实现特征的稳定初始化和更新，比如设置第一层和第二层设置不同的学习率来分别实现各层好的特征学习效果。<strong>muP 的解决思路正是如此：在模型变大的过程中，通过分层、科学地调整（迁移）小模型上得到的最优超参（初始化、学习率等），实现大模型特征学习的稳定与高效（Maximal Update）</strong>。我们后续的文章就将逐渐进行展开介绍。</p> <h2 id="总结">总结</h2> <p>本文首先提出大模型训练特征学习能力渐进失效和大模型调参困难、超参难以从小模型迁移到大模型的两大难题，并在一个简单例子上从理论角度说明了这两大难题，最后简单阐述了 muP 的解决思路。</p>]]></content><author><name></name></author><category term="muP-tutorial"/><category term="scaling"/><category term="optimization"/><category term="muP"/><summary type="html"><![CDATA[介绍大模型训练的两大难题，简单理论分析 standard practice 的缺陷，从而引出 muP 概念并介绍其作用。]]></summary></entry></feed>